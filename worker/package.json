{
  "name": "nappio-pairing-worker",
  "description": "Cloudflare pairing, signaling, session, and TURN credential service for NapNear.",
  "version": "1.0.0",
  "author": "Tim Moreton",
  "license": "Apache-2.0",
  "homepage": "https://github.com/tmoreton/nappio#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tmoreton/nappio.git",
    "directory": "worker"
  },
  "engines": {
    "node": ">=22.13"
  },
  "overrides": {
    "sharp": "0.35.4"
  },
  "private": true,
  "type": "module",
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/vitest-plugin": "^1.1.5",
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11",
    "wrangler": "^4.129.1"
  },
  "scripts": {
    "dev": "wrangler dev --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc -p test/tsconfig.json --noEmit"
  }
}
